using GDash.DB;
using GDash.MVVM.Model;
using Moq;
using NUnit.Framework;
using System.Collections.Generic;
using System.Data;


namespace GDash.Tests
{
    public class EssayConnTests
    {
        /*
        [Test]
        public void GetAllDB_ValidCall()
        {
            Mock<IConnection> npg_mock = new Mock<IConnection>();

            npg_mock.Setup(rep => rep.GetConnection()).Returns(It.IsAny<IDbConnection>());
            npg_mock.Setup(rep => rep.GetCommand()).Returns(It.IsAny<IDbCommand>());

            EssayConn essayConn = new EssayConn(npg_mock.Object);

            List<IModel> users = essayConn.GetAllDB();
            Assert.NotNull(users);

            Assert.Pass();
        }

        [Test]
        public void InsertDB_ValidCall()
        {
            Mock<IConnection> npg_mock = new Mock<IConnection>();

            npg_mock.Setup(rep => rep.GetConnection()).Returns(It.IsAny<IDbConnection>());
            npg_mock.Setup(rep => rep.GetCommand()).Returns(It.IsAny<IDbCommand>());

            EssayConn essayConn = new EssayConn(npg_mock.Object);
            Essay essay = new Essay();
            essayConn.InsertDB(essay);

            Assert.Pass();
        }

        [Test]
        public void UpdateDB_ValidCall()
        {
            Mock<IConnection> npg_mock = new Mock<IConnection>();

            npg_mock.Setup(rep => rep.GetConnection()).Returns(It.IsAny<IDbConnection>());
            npg_mock.Setup(rep => rep.GetCommand()).Returns(It.IsAny<IDbCommand>());

            EssayConn essayConn = new EssayConn(npg_mock.Object);
            Essay essay = new Essay();
            essayConn.UpdateDB(essay);

            Assert.Pass();
        }

        [Test]
        public void DeleteDB_ValidCall()
        {
            Mock<IConnection> npg_mock = new Mock<IConnection>();

            npg_mock.Setup(rep => rep.GetConnection()).Returns(It.IsAny<IDbConnection>());
            npg_mock.Setup(rep => rep.GetCommand()).Returns(It.IsAny<IDbCommand>());

            EssayConn essayConn = new EssayConn(npg_mock.Object);
            Essay essay = new Essay();
            essayConn.DeleteDB(essay);

            Assert.Pass();
        }

        
        [Test]
        public void HasUser_ValidCall()
        {
            Mock<IConnection> npg_mock = new Mock<IConnection>();

            npg_mock.Setup(rep => rep.GetConnection()).Returns(It.IsAny<IDbConnection>());
            npg_mock.Setup(rep => rep.GetCommand()).Returns(It.IsAny<IDbCommand>());

            EssayConn essayConn = new EssayConn(npg_mock.Object);
            bool hasUser = essayConn.HasUser();

            Assert.Pass();
        }

                
        */
    }
}
